// Compatibility export. The implementation lives in the shared repository so
// every extension context uses the same schema, chunker, search and cleanup rules.
export * from '../shared/documentRepository';
